$-- Implements the frontmatter, title page, and abstract.
$--
$-- %%%% TODO %%%%%
$-- Customize is needed, otherwise remove this partials to use Quarto default one
$-- %%%%%%%%%%%%%%%%
$if(has-frontmatter)$
\frontmatter
$endif$

$if(abstract)$
\renewcommand{\maketitlehookd}{%
\begin{abstract}
\noindent{}$abstract$
$if(document-id)$
\\[1em]
\emph{\small{}\textbf{Document-ID:} $document-id$}
$endif$
\end{abstract}
}
$endif$

$if(title)$
\maketitle
$endif$
